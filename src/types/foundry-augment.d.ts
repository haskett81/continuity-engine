// Local augmentation for gaps in @league-of-foundry-developers/foundry-vtt-types.
// The installed package (pinned in package.json) only has full coverage through
// Foundry v13; this module targets v14. When the compiler flags something as
// missing or wrong here rather than at the call site, add it below with a
// comment linking the relevant Foundry API doc page. Never inline-cast with
// `any` at the call site instead of augmenting here — see docs/continuity-engine-spec.md §8.
export {};
